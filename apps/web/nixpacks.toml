[phases.setup]
nixPkgs = ["nodejs_22"]
aptPkgs = []

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@9.0.0 --activate"
]

[start]
cmd = "echo 'Start command is configured in railway.json'"
